# Used automatically by `vite build` (production / Vercel deploys).
# Local `npm run dev` keeps using frontend/.env (localhost) instead.
# If your Render backend URL differs, update it here OR set VITE_API_BASE_URL
# in the Vercel project settings (dashboard env vars override this file).
VITE_API_BASE_URL=https://fitflow-3-is91.onrender.com
